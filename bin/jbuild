
(jbuild_version 1)

(executables (
  (names (real_world demo csv_read))
  (public_names (real_world demo csv_read))
  (libraries (lib core lwt lwt_ppx lwt.unix csv))
  (preprocess (pps (lwt_ppx)))))
